#!/bin/bash

ulimit -n 1048576 # open files

mkdir -p ~/n8n/appdata

echo "N8N_VERSION=${n8nVersion}" >> ~/n8n/.env
echo "DB_POSTGRESDB_HOST=${postgresHost}" >> ~/n8n/.env
echo "QUEUE_BULL_REDIS_HOST=${redisHost}" >> ~/n8n/.env

sleep 1

sudo docker-compose -f ~/n8n/docker-compose.yml up -d
